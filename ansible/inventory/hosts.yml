all:
  children:
    jenkins_server:
      hosts:
        jenkins-server:
          ansible_host: DROPLET_IP   # remplacé automatiquement par provision_droplet.yml
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
